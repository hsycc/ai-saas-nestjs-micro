generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PG_DATABASE_AI")
}

model TestModel {
  id String @id @unique @default(cuid())

  @@map("test_model")
}
